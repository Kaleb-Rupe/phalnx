/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getConstraintEntryZCDecoder,
  getConstraintEntryZCEncoder,
  type ConstraintEntryZC,
  type ConstraintEntryZCArgs,
} from "../types/index.js";

export const PENDING_CONSTRAINTS_UPDATE_DISCRIMINATOR = new Uint8Array([
  22, 206, 77, 208, 147, 121, 53, 174,
]);

export function getPendingConstraintsUpdateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PENDING_CONSTRAINTS_UPDATE_DISCRIMINATOR,
  );
}

export type PendingConstraintsUpdate = {
  discriminator: ReadonlyUint8Array;
  /** Associated vault pubkey (as raw bytes for Pod compatibility) */
  vault: ReadonlyUint8Array;
  /** New constraint entries to apply (fixed array, use entry_count for active) */
  entries: Array<ConstraintEntryZC>;
  /** Number of active entries (0..=64) */
  entryCount: number;
  /** Bump seed for PDA */
  bump: number;
  /**
   * Alignment padding. Total: 8+32+35840+1+1+6 = 35888, keeping queued_at at
   * struct offset 35880 (8-aligned post-discriminator absolute 35888).
   */
  padding: ReadonlyUint8Array;
  /** Unix timestamp when this update was queued */
  queuedAt: bigint;
  /** Unix timestamp when this update becomes executable */
  executesAt: bigint;
  /**
   * Slot number when this update was queued. Paired with `MAX_APPLY_AGE_SLOTS`
   * to enforce a freshness ceiling — defends against durable-nonce pre-signing
   * attacks (F-10 audit fix, Drift Protocol April 2026 $285M analog).
   * Already 8-byte aligned (follows two i64 fields).
   */
  queuedAtSlot: bigint;
  /**
   * M-4 close (Bucket 2, Phase 10 PEN-CROSS-3): SHA-256 over the canonical
   * byte encoding of the pending content (vault + entry_count + active
   * entries[0..entry_count]). Written once at `queue_constraints_update`
   * and re-asserted at `apply_constraints_update` before any byte is
   * copied into the live `InstructionConstraints` PDA.
   *
   * Defense-in-depth against discriminator-collision overwrite of this
   * pending PDA's body between queue and apply: even if a future bug
   * allowed a same-seed CPI to rewrite the entries slab, the digest
   * recorded at queue time pins the owner-attested content, and the
   * apply-time recompute would diverge and reject with
   * `ErrPendingConstraintsDigestMismatch`.
   *
   * Alignment: follows a u64 at struct offset 35896, so byte offset
   * 35904..35936 is 8-aligned. `[u8; 32]` has alignment 1, no padding
   * required.
   */
  pendingContentDigest: ReadonlyUint8Array;
};

export type PendingConstraintsUpdateArgs = {
  /** Associated vault pubkey (as raw bytes for Pod compatibility) */
  vault: ReadonlyUint8Array;
  /** New constraint entries to apply (fixed array, use entry_count for active) */
  entries: Array<ConstraintEntryZCArgs>;
  /** Number of active entries (0..=64) */
  entryCount: number;
  /** Bump seed for PDA */
  bump: number;
  /**
   * Alignment padding. Total: 8+32+35840+1+1+6 = 35888, keeping queued_at at
   * struct offset 35880 (8-aligned post-discriminator absolute 35888).
   */
  padding: ReadonlyUint8Array;
  /** Unix timestamp when this update was queued */
  queuedAt: number | bigint;
  /** Unix timestamp when this update becomes executable */
  executesAt: number | bigint;
  /**
   * Slot number when this update was queued. Paired with `MAX_APPLY_AGE_SLOTS`
   * to enforce a freshness ceiling — defends against durable-nonce pre-signing
   * attacks (F-10 audit fix, Drift Protocol April 2026 $285M analog).
   * Already 8-byte aligned (follows two i64 fields).
   */
  queuedAtSlot: number | bigint;
  /**
   * M-4 close (Bucket 2, Phase 10 PEN-CROSS-3): SHA-256 over the canonical
   * byte encoding of the pending content (vault + entry_count + active
   * entries[0..entry_count]). Written once at `queue_constraints_update`
   * and re-asserted at `apply_constraints_update` before any byte is
   * copied into the live `InstructionConstraints` PDA.
   *
   * Defense-in-depth against discriminator-collision overwrite of this
   * pending PDA's body between queue and apply: even if a future bug
   * allowed a same-seed CPI to rewrite the entries slab, the digest
   * recorded at queue time pins the owner-attested content, and the
   * apply-time recompute would diverge and reject with
   * `ErrPendingConstraintsDigestMismatch`.
   *
   * Alignment: follows a u64 at struct offset 35896, so byte offset
   * 35904..35936 is 8-aligned. `[u8; 32]` has alignment 1, no padding
   * required.
   */
  pendingContentDigest: ReadonlyUint8Array;
};

/** Gets the encoder for {@link PendingConstraintsUpdateArgs} account data. */
export function getPendingConstraintsUpdateEncoder(): FixedSizeEncoder<PendingConstraintsUpdateArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["vault", fixEncoderSize(getBytesEncoder(), 32)],
      ["entries", getArrayEncoder(getConstraintEntryZCEncoder(), { size: 64 })],
      ["entryCount", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["padding", fixEncoderSize(getBytesEncoder(), 6)],
      ["queuedAt", getI64Encoder()],
      ["executesAt", getI64Encoder()],
      ["queuedAtSlot", getU64Encoder()],
      ["pendingContentDigest", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({
      ...value,
      discriminator: PENDING_CONSTRAINTS_UPDATE_DISCRIMINATOR,
    }),
  );
}

/** Gets the decoder for {@link PendingConstraintsUpdate} account data. */
export function getPendingConstraintsUpdateDecoder(): FixedSizeDecoder<PendingConstraintsUpdate> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["vault", fixDecoderSize(getBytesDecoder(), 32)],
    ["entries", getArrayDecoder(getConstraintEntryZCDecoder(), { size: 64 })],
    ["entryCount", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["padding", fixDecoderSize(getBytesDecoder(), 6)],
    ["queuedAt", getI64Decoder()],
    ["executesAt", getI64Decoder()],
    ["queuedAtSlot", getU64Decoder()],
    ["pendingContentDigest", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

/** Gets the codec for {@link PendingConstraintsUpdate} account data. */
export function getPendingConstraintsUpdateCodec(): FixedSizeCodec<
  PendingConstraintsUpdateArgs,
  PendingConstraintsUpdate
> {
  return combineCodec(
    getPendingConstraintsUpdateEncoder(),
    getPendingConstraintsUpdateDecoder(),
  );
}

export function decodePendingConstraintsUpdate<
  TAddress extends string = string,
>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<PendingConstraintsUpdate, TAddress>;
export function decodePendingConstraintsUpdate<
  TAddress extends string = string,
>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<PendingConstraintsUpdate, TAddress>;
export function decodePendingConstraintsUpdate<
  TAddress extends string = string,
>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<PendingConstraintsUpdate, TAddress>
  | MaybeAccount<PendingConstraintsUpdate, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getPendingConstraintsUpdateDecoder(),
  );
}

export async function fetchPendingConstraintsUpdate<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<PendingConstraintsUpdate, TAddress>> {
  const maybeAccount = await fetchMaybePendingConstraintsUpdate(
    rpc,
    address,
    config,
  );
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybePendingConstraintsUpdate<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<PendingConstraintsUpdate, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodePendingConstraintsUpdate(maybeAccount);
}

export async function fetchAllPendingConstraintsUpdate(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<PendingConstraintsUpdate>[]> {
  const maybeAccounts = await fetchAllMaybePendingConstraintsUpdate(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybePendingConstraintsUpdate(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<PendingConstraintsUpdate>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodePendingConstraintsUpdate(maybeAccount),
  );
}

export function getPendingConstraintsUpdateSize(): number {
  return 35944;
}
