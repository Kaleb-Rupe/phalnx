/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SIGIL_PROGRAM_ADDRESS } from "../programs/index.js";

export const CLEANUP_ORPHAN_CONSTRAINTS_PDA_DISCRIMINATOR = new Uint8Array([
  69, 181, 93, 235, 116, 229, 116, 4,
]);

export function getCleanupOrphanConstraintsPdaDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLEANUP_ORPHAN_CONSTRAINTS_PDA_DISCRIMINATOR,
  );
}

export type CleanupOrphanConstraintsPdaInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountPolicy extends string | AccountMeta<string> = string,
  TAccountOrphanPda extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountVault extends string
        ? ReadonlyAccount<TAccountVault>
        : TAccountVault,
      TAccountPolicy extends string
        ? ReadonlyAccount<TAccountPolicy>
        : TAccountPolicy,
      TAccountOrphanPda extends string
        ? WritableAccount<TAccountOrphanPda>
        : TAccountOrphanPda,
      ...TRemainingAccounts,
    ]
  >;

export type CleanupOrphanConstraintsPdaInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CleanupOrphanConstraintsPdaInstructionDataArgs = {};

export function getCleanupOrphanConstraintsPdaInstructionDataEncoder(): FixedSizeEncoder<CleanupOrphanConstraintsPdaInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CLEANUP_ORPHAN_CONSTRAINTS_PDA_DISCRIMINATOR,
    }),
  );
}

export function getCleanupOrphanConstraintsPdaInstructionDataDecoder(): FixedSizeDecoder<CleanupOrphanConstraintsPdaInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCleanupOrphanConstraintsPdaInstructionDataCodec(): FixedSizeCodec<
  CleanupOrphanConstraintsPdaInstructionDataArgs,
  CleanupOrphanConstraintsPdaInstructionData
> {
  return combineCodec(
    getCleanupOrphanConstraintsPdaInstructionDataEncoder(),
    getCleanupOrphanConstraintsPdaInstructionDataDecoder(),
  );
}

export type CleanupOrphanConstraintsPdaAsyncInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
  TAccountPolicy extends string = string,
  TAccountOrphanPda extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault: Address<TAccountVault>;
  policy?: Address<TAccountPolicy>;
  /**
   * by this program, and discriminator zero (orphan from incomplete
   * allocate+extend+populate chain).
   */
  orphanPda?: Address<TAccountOrphanPda>;
};

export async function getCleanupOrphanConstraintsPdaInstructionAsync<
  TAccountOwner extends string,
  TAccountVault extends string,
  TAccountPolicy extends string,
  TAccountOrphanPda extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: CleanupOrphanConstraintsPdaAsyncInput<
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountOrphanPda
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CleanupOrphanConstraintsPdaInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountOrphanPda
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: false },
    policy: { value: input.policy ?? null, isWritable: false },
    orphanPda: { value: input.orphanPda ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.policy.value) {
    accounts.policy.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([112, 111, 108, 105, 99, 121])),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "vault",
            accounts.vault.value,
          ),
        ),
      ],
    });
  }
  if (!accounts.orphanPda.value) {
    accounts.orphanPda.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([99, 111, 110, 115, 116, 114, 97, 105, 110, 116, 115]),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "vault",
            accounts.vault.value,
          ),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("policy", accounts.policy),
      getAccountMeta("orphanPda", accounts.orphanPda),
    ],
    data: getCleanupOrphanConstraintsPdaInstructionDataEncoder().encode({}),
    programAddress,
  } as CleanupOrphanConstraintsPdaInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountOrphanPda
  >);
}

export type CleanupOrphanConstraintsPdaInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
  TAccountPolicy extends string = string,
  TAccountOrphanPda extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault: Address<TAccountVault>;
  policy: Address<TAccountPolicy>;
  /**
   * by this program, and discriminator zero (orphan from incomplete
   * allocate+extend+populate chain).
   */
  orphanPda: Address<TAccountOrphanPda>;
};

export function getCleanupOrphanConstraintsPdaInstruction<
  TAccountOwner extends string,
  TAccountVault extends string,
  TAccountPolicy extends string,
  TAccountOrphanPda extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: CleanupOrphanConstraintsPdaInput<
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountOrphanPda
  >,
  config?: { programAddress?: TProgramAddress },
): CleanupOrphanConstraintsPdaInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountVault,
  TAccountPolicy,
  TAccountOrphanPda
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: false },
    policy: { value: input.policy ?? null, isWritable: false },
    orphanPda: { value: input.orphanPda ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("policy", accounts.policy),
      getAccountMeta("orphanPda", accounts.orphanPda),
    ],
    data: getCleanupOrphanConstraintsPdaInstructionDataEncoder().encode({}),
    programAddress,
  } as CleanupOrphanConstraintsPdaInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountPolicy,
    TAccountOrphanPda
  >);
}

export type ParsedCleanupOrphanConstraintsPdaInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    vault: TAccountMetas[1];
    policy: TAccountMetas[2];
    /**
     * by this program, and discriminator zero (orphan from incomplete
     * allocate+extend+populate chain).
     */
    orphanPda: TAccountMetas[3];
  };
  data: CleanupOrphanConstraintsPdaInstructionData;
};

export function parseCleanupOrphanConstraintsPdaInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCleanupOrphanConstraintsPdaInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      vault: getNextAccount(),
      policy: getNextAccount(),
      orphanPda: getNextAccount(),
    },
    data: getCleanupOrphanConstraintsPdaInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
