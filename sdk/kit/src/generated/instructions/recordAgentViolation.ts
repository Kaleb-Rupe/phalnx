/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SIGIL_PROGRAM_ADDRESS } from "../programs/index.js";

export const RECORD_AGENT_VIOLATION_DISCRIMINATOR = new Uint8Array([
  131, 113, 120, 227, 219, 36, 160, 109,
]);

export function getRecordAgentViolationDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RECORD_AGENT_VIOLATION_DISCRIMINATOR,
  );
}

export type RecordAgentViolationInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountPolicy extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountPolicy extends string
        ? WritableAccount<TAccountPolicy>
        : TAccountPolicy,
      ...TRemainingAccounts,
    ]
  >;

export type RecordAgentViolationInstructionData = {
  discriminator: ReadonlyUint8Array;
  agent: Address;
  errorCode: number;
};

export type RecordAgentViolationInstructionDataArgs = {
  agent: Address;
  errorCode: number;
};

export function getRecordAgentViolationInstructionDataEncoder(): FixedSizeEncoder<RecordAgentViolationInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["agent", getAddressEncoder()],
      ["errorCode", getU32Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: RECORD_AGENT_VIOLATION_DISCRIMINATOR,
    }),
  );
}

export function getRecordAgentViolationInstructionDataDecoder(): FixedSizeDecoder<RecordAgentViolationInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["agent", getAddressDecoder()],
    ["errorCode", getU32Decoder()],
  ]);
}

export function getRecordAgentViolationInstructionDataCodec(): FixedSizeCodec<
  RecordAgentViolationInstructionDataArgs,
  RecordAgentViolationInstructionData
> {
  return combineCodec(
    getRecordAgentViolationInstructionDataEncoder(),
    getRecordAgentViolationInstructionDataDecoder(),
  );
}

export type RecordAgentViolationAsyncInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
  TAccountPolicy extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault: Address<TAccountVault>;
  policy?: Address<TAccountPolicy>;
  agent: RecordAgentViolationInstructionDataArgs["agent"];
  errorCode: RecordAgentViolationInstructionDataArgs["errorCode"];
};

export async function getRecordAgentViolationInstructionAsync<
  TAccountOwner extends string,
  TAccountVault extends string,
  TAccountPolicy extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: RecordAgentViolationAsyncInput<
    TAccountOwner,
    TAccountVault,
    TAccountPolicy
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RecordAgentViolationInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountPolicy
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
    policy: { value: input.policy ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.policy.value) {
    accounts.policy.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([112, 111, 108, 105, 99, 121])),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "vault",
            accounts.vault.value,
          ),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("policy", accounts.policy),
    ],
    data: getRecordAgentViolationInstructionDataEncoder().encode(
      args as RecordAgentViolationInstructionDataArgs,
    ),
    programAddress,
  } as RecordAgentViolationInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountPolicy
  >);
}

export type RecordAgentViolationInput<
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
  TAccountPolicy extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  vault: Address<TAccountVault>;
  policy: Address<TAccountPolicy>;
  agent: RecordAgentViolationInstructionDataArgs["agent"];
  errorCode: RecordAgentViolationInstructionDataArgs["errorCode"];
};

export function getRecordAgentViolationInstruction<
  TAccountOwner extends string,
  TAccountVault extends string,
  TAccountPolicy extends string,
  TProgramAddress extends Address = typeof SIGIL_PROGRAM_ADDRESS,
>(
  input: RecordAgentViolationInput<
    TAccountOwner,
    TAccountVault,
    TAccountPolicy
  >,
  config?: { programAddress?: TProgramAddress },
): RecordAgentViolationInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountVault,
  TAccountPolicy
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIGIL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
    policy: { value: input.policy ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("policy", accounts.policy),
    ],
    data: getRecordAgentViolationInstructionDataEncoder().encode(
      args as RecordAgentViolationInstructionDataArgs,
    ),
    programAddress,
  } as RecordAgentViolationInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountVault,
    TAccountPolicy
  >);
}

export type ParsedRecordAgentViolationInstruction<
  TProgram extends string = typeof SIGIL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    vault: TAccountMetas[1];
    policy: TAccountMetas[2];
  };
  data: RecordAgentViolationInstructionData;
};

export function parseRecordAgentViolationInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRecordAgentViolationInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      vault: getNextAccount(),
      policy: getNextAccount(),
    },
    data: getRecordAgentViolationInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
