/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/**
 * Per-agent contribution entry within an overlay.
 * Tracks each agent's individual spend contributions using a 24-bucket
 * hourly epoch scheme with per-entry `last_write_epoch` for correct gap-zeroing.
 *
 * Layout: 32 (agent) + 8 (last_write_epoch) + 8 × 24 (contributions) = 232 bytes
 */
export type AgentContributionEntry = {
  /** Agent pubkey stored as raw bytes (zero_copy requires fixed-size) */
  agent: ReadonlyUint8Array;
  /**
   * The epoch number of the most recent write to this entry.
   * Used to derive which buckets are stale via modular arithmetic.
   * epoch = unix_timestamp / OVERLAY_EPOCH_DURATION (3600)
   */
  lastWriteEpoch: bigint;
  /**
   * Per-epoch USD contributions from this agent.
   * Indexed by `epoch % OVERLAY_NUM_EPOCHS`.
   */
  contributions: Array<bigint>;
};

export type AgentContributionEntryArgs = {
  /** Agent pubkey stored as raw bytes (zero_copy requires fixed-size) */
  agent: ReadonlyUint8Array;
  /**
   * The epoch number of the most recent write to this entry.
   * Used to derive which buckets are stale via modular arithmetic.
   * epoch = unix_timestamp / OVERLAY_EPOCH_DURATION (3600)
   */
  lastWriteEpoch: number | bigint;
  /**
   * Per-epoch USD contributions from this agent.
   * Indexed by `epoch % OVERLAY_NUM_EPOCHS`.
   */
  contributions: Array<number | bigint>;
};

export function getAgentContributionEntryEncoder(): FixedSizeEncoder<AgentContributionEntryArgs> {
  return getStructEncoder([
    ["agent", fixEncoderSize(getBytesEncoder(), 32)],
    ["lastWriteEpoch", getI64Encoder()],
    ["contributions", getArrayEncoder(getU64Encoder(), { size: 24 })],
  ]);
}

export function getAgentContributionEntryDecoder(): FixedSizeDecoder<AgentContributionEntry> {
  return getStructDecoder([
    ["agent", fixDecoderSize(getBytesDecoder(), 32)],
    ["lastWriteEpoch", getI64Decoder()],
    ["contributions", getArrayDecoder(getU64Decoder(), { size: 24 })],
  ]);
}

export function getAgentContributionEntryCodec(): FixedSizeCodec<
  AgentContributionEntryArgs,
  AgentContributionEntry
> {
  return combineCodec(
    getAgentContributionEntryEncoder(),
    getAgentContributionEntryDecoder(),
  );
}
