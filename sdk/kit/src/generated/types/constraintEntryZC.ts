/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAccountConstraintZCDecoder,
  getAccountConstraintZCEncoder,
  getDataConstraintZCDecoder,
  getDataConstraintZCEncoder,
  type AccountConstraintZC,
  type AccountConstraintZCArgs,
  type DataConstraintZC,
  type DataConstraintZCArgs,
} from "./index.js";

/**
 * BYTE LAYOUT REGISTRY — Canonical assignment of padding bytes.
 *
 * Layout (post is_spending removal, M2 Option A — runtime never read it):
 *
 * byte 554: _reserved_was_is_spending  (was: is_spending; deleted M2 Option A)
 * byte 555: discriminator_format
 * bytes 556-559: _padding[4]  (reserved for future use)
 *
 * Total: 32+320+200+1+1+1+1+4 = 560 (unchanged).
 * is_spending (formerly byte 554) was deleted because the runtime never reads
 * it — `validate_and_authorize.rs:134` derives spending from `amount > 0`.
 * The freed byte is held as `_reserved_was_is_spending` to preserve the
 * 560-byte total — shrinking the struct would corrupt every existing
 * on-chain InstructionConstraints PDA (35,888-byte zero-copy account).
 */
export type ConstraintEntryZC = {
  programId: ReadonlyUint8Array;
  dataConstraints: Array<DataConstraintZC>;
  accountConstraints: Array<AccountConstraintZC>;
  dataCount: number;
  accountCount: number;
  /**
   * Reserved byte — formerly `is_spending` (deleted M2 Option A: runtime
   * never read it; spending is derived from `amount > 0`). Held as
   * padding to preserve the 560-byte ConstraintEntryZC invariant; existing
   * on-chain PDAs zero-init this byte. Do not repurpose without a
   * migration plan — old PDAs will read 0/1/2 here from prior writes.
   */
  reservedWasIsSpending: number;
  /**
   * DiscriminatorFormat discriminant (0=Anchor8, 1=Spl1). Write-time only —
   * verify_data_constraints_zc() does not read this field at runtime.
   * Zero-initialized on existing V1 PDAs → 0 → Anchor8 (backward compatible).
   */
  discriminatorFormat: number;
  padding: ReadonlyUint8Array;
};

export type ConstraintEntryZCArgs = {
  programId: ReadonlyUint8Array;
  dataConstraints: Array<DataConstraintZCArgs>;
  accountConstraints: Array<AccountConstraintZCArgs>;
  dataCount: number;
  accountCount: number;
  /**
   * Reserved byte — formerly `is_spending` (deleted M2 Option A: runtime
   * never read it; spending is derived from `amount > 0`). Held as
   * padding to preserve the 560-byte ConstraintEntryZC invariant; existing
   * on-chain PDAs zero-init this byte. Do not repurpose without a
   * migration plan — old PDAs will read 0/1/2 here from prior writes.
   */
  reservedWasIsSpending: number;
  /**
   * DiscriminatorFormat discriminant (0=Anchor8, 1=Spl1). Write-time only —
   * verify_data_constraints_zc() does not read this field at runtime.
   * Zero-initialized on existing V1 PDAs → 0 → Anchor8 (backward compatible).
   */
  discriminatorFormat: number;
  padding: ReadonlyUint8Array;
};

export function getConstraintEntryZCEncoder(): FixedSizeEncoder<ConstraintEntryZCArgs> {
  return getStructEncoder([
    ["programId", fixEncoderSize(getBytesEncoder(), 32)],
    [
      "dataConstraints",
      getArrayEncoder(getDataConstraintZCEncoder(), { size: 8 }),
    ],
    [
      "accountConstraints",
      getArrayEncoder(getAccountConstraintZCEncoder(), { size: 5 }),
    ],
    ["dataCount", getU8Encoder()],
    ["accountCount", getU8Encoder()],
    ["reservedWasIsSpending", getU8Encoder()],
    ["discriminatorFormat", getU8Encoder()],
    ["padding", fixEncoderSize(getBytesEncoder(), 4)],
  ]);
}

export function getConstraintEntryZCDecoder(): FixedSizeDecoder<ConstraintEntryZC> {
  return getStructDecoder([
    ["programId", fixDecoderSize(getBytesDecoder(), 32)],
    [
      "dataConstraints",
      getArrayDecoder(getDataConstraintZCDecoder(), { size: 8 }),
    ],
    [
      "accountConstraints",
      getArrayDecoder(getAccountConstraintZCDecoder(), { size: 5 }),
    ],
    ["dataCount", getU8Decoder()],
    ["accountCount", getU8Decoder()],
    ["reservedWasIsSpending", getU8Decoder()],
    ["discriminatorFormat", getU8Decoder()],
    ["padding", fixDecoderSize(getBytesDecoder(), 4)],
  ]);
}

export function getConstraintEntryZCCodec(): FixedSizeCodec<
  ConstraintEntryZCArgs,
  ConstraintEntryZC
> {
  return combineCodec(
    getConstraintEntryZCEncoder(),
    getConstraintEntryZCDecoder(),
  );
}
