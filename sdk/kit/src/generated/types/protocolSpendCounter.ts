/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/**
 * Per-protocol spend counter using simple 24h window.
 * When current_epoch - window_start >= 144, the window is expired and resets to 0.
 * 48 bytes per entry (32 + 8 + 8).
 */
export type ProtocolSpendCounter = {
  /** Protocol program ID */
  protocol: ReadonlyUint8Array;
  /** Window start timestamp (for future rolling window) */
  windowStart: bigint;
  /** Accumulated spend in window (for future cap enforcement) */
  windowSpend: bigint;
};

export type ProtocolSpendCounterArgs = {
  /** Protocol program ID */
  protocol: ReadonlyUint8Array;
  /** Window start timestamp (for future rolling window) */
  windowStart: number | bigint;
  /** Accumulated spend in window (for future cap enforcement) */
  windowSpend: number | bigint;
};

export function getProtocolSpendCounterEncoder(): FixedSizeEncoder<ProtocolSpendCounterArgs> {
  return getStructEncoder([
    ["protocol", fixEncoderSize(getBytesEncoder(), 32)],
    ["windowStart", getI64Encoder()],
    ["windowSpend", getU64Encoder()],
  ]);
}

export function getProtocolSpendCounterDecoder(): FixedSizeDecoder<ProtocolSpendCounter> {
  return getStructDecoder([
    ["protocol", fixDecoderSize(getBytesDecoder(), 32)],
    ["windowStart", getI64Decoder()],
    ["windowSpend", getU64Decoder()],
  ]);
}

export function getProtocolSpendCounterCodec(): FixedSizeCodec<
  ProtocolSpendCounterArgs,
  ProtocolSpendCounter
> {
  return combineCodec(
    getProtocolSpendCounterEncoder(),
    getProtocolSpendCounterDecoder(),
  );
}
